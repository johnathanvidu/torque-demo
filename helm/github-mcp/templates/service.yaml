apiVersion: v1
kind: Service
metadata:
  # Fixed name so the MCP endpoint DNS is deterministic:
  #   http://github-mcp.<namespace>.svc.cluster.local:<port>/
  name: github-mcp
  labels:
    app: github-mcp
spec:
  type: {{ .Values.service.type }}
  selector:
    app: github-mcp
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
