apiVersion: v1
kind: Service
metadata:
  # Fixed name so the MCP endpoint DNS is deterministic:
  #   http://jira-mcp.<namespace>.svc.cluster.local:<port>/mcp
  name: jira-mcp
  labels:
    app: jira-mcp
spec:
  type: {{ .Values.service.type }}
  selector:
    app: jira-mcp
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
