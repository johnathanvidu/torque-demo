apiVersion: v1
kind: Service
metadata:
  # Name carries the instance suffix, so several releases can share a namespace
  # and each still gets its own deterministic DNS record:
  #   http://<chart>-<instance>.<namespace>.svc.cluster.local:<port>/mcp
  # The blueprint's mcp_endpoint output builds the same string, and the
  # --allowed-hosts list in the Deployment is computed from it too.
  name: {{ include "grafana-mcp.fullname" . }}
  labels:
    {{- include "grafana-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "grafana-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
