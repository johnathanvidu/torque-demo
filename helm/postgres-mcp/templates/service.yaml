apiVersion: v1
kind: Service
metadata:
  # Fixed name so the MCP endpoint DNS is deterministic:
  #   http://postgres-mcp.<namespace>.svc.cluster.local:<port>/mcp
  name: postgres-mcp
  labels:
    app: postgres-mcp
spec:
  type: {{ .Values.service.type }}
  selector:
    app: postgres-mcp
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
